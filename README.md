# sai

## conclusion/output.py
Тут реализация прямого и обратного вывода. Это было отдельное задание. Нигде не используется.

## statements/
- `statement.py` — реализация класса нечеткого высказывания
- `fuzzyset.py` — реализация класса нечеткого множества и отдельно треугольного и трапециевидного множества
- `main.py` — тестовые функции, использующие эти классы

## color.py
Реализация модели Мамдани для задачи классификации цветов. Работает, но нужно убедиться, что это то самое.

Основное решение для упрощения зашито прямо в классы множеств в `fuzzyset.py`.

## phone.py
Была еще задача для выбора телефона среди разных в зависимости от параметров:
- цена
- камера
- память
- диагональ экрана
- и т. д.

Но достоверно неизвестно, какая это модель. Пока не реализовано.

## sugeno*.py
- `sugeno.py` — модель Сугено (не сильно проверено)
- `sugenotest.py` — тесты для Сугено
- `sugenoappr.py` — тест для Сугено с экзамена
